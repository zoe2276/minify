import loadingIcon from "../graphics/loading.png"

export const Loading = () => {
    return <div className="loadingIcon-wrapper"><img src={loadingIcon} className="loadingIcon" alt="Loading..."></img></div>
}